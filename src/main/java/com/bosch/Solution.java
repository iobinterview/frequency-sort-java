package com.bosch;

import java.util.*;

public class Solution {

    /**
     * Sort the input according to the frequency of elements in decreasing order.
     * If the frequency of elements is the same, then sort these elements in increasing order by their values.
     */
    public static List<Integer> sortByFrequency(List<Integer> input) {

        return new ArrayList<>();
    }
}
